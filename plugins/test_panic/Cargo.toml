[package]
name = "test_panic"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
plugin_abi = { path = "../../crates/plugin_abi" }

[profile.release]
opt-level = "s"
lto = true
